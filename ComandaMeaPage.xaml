<Page x:Class="AutoPartsShop.ComandaMeaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="760" d:DesignWidth="980"
      Title="ComandaMeaPage" Background="#F7FAFC" FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Comanda mea" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                <TextBlock Text="Produsele adaugate in comanda si totalul de plata." FontSize="14" Foreground="#718096" Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Reincarca" Click="BtnReincarca_Click" Style="{StaticResource ActionButton}" Background="#2D3748" Foreground="White" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12">
            <DataGrid x:Name="ComenziGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataComanda, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="145"/>
                    <DataGridTextColumn Header="Produs" Binding="{Binding Produs}" Width="2*"/>
                    <DataGridTextColumn Header="Categorie" Binding="{Binding Categorie}" Width="140"/>
                    <DataGridTextColumn Header="Cantitate" Binding="{Binding Cantitate}" Width="90"/>
                    <DataGridTextColumn Header="Pret" Binding="{Binding Pret, StringFormat={}{0:0.00} lei}" Width="100"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:0.00} lei}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TxtNumarProduse" Foreground="#4A5568" FontSize="15" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Finalizeaza comanda" Click="BtnFinalizeazaComanda_Click" Style="{StaticResource ActionButton}" Background="#3182CE" Foreground="White"/>
                <TextBlock x:Name="TxtTotalComenzi" Grid.Column="2" Foreground="#1A202C" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>
