<Page x:Class="AutoPartsShop.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="760" d:DesignWidth="980"
      Title="MainPage" Background="#F7FAFC" FontFamily="Segoe UI">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F7FAFC" Offset="0"/>
                <GradientStop Color="#EDF2F7" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="20">

                <Border Background="#3182CE" CornerRadius="12" Padding="30" Margin="0,0,0,25">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="4" Opacity="0.22"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Găsește piesele potrivite pentru mașina ta!" Foreground="White" FontSize="26" FontWeight="Bold"/>
                            <TextBlock Text="Peste 50.000 de piese auto și accesorii pe stoc, cu livrare rapidă." Foreground="#EBF8FF" FontSize="14" Margin="0,8,0,0"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="🚗" FontSize="60" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </Grid>
                </Border>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="8,0,0,8" BorderBrush="#E2E8F0" BorderThickness="1,1,0,1">
                        <TextBox x:Name="SearchBox" FontSize="16" Padding="15,10" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center"/>
                    </Border>

                    <TextBlock Grid.Column="0" Text="Ex: Plăcuțe frână Golf 7..." Foreground="#A0AEC0" FontSize="16" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button x:Name="BtnCauta" Grid.Column="1" Content="🔍 Caută" Background="#1A202C" Foreground="White" FontSize="16" FontWeight="SemiBold" Cursor="Hand" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="0,8,8,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <TextBlock Text="Categorii Populare" FontSize="22" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,15"/>

                <WrapPanel Margin="-10">
                    <Border Background="White" Width="200" Height="120" CornerRadius="10" Margin="10" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="🛢️" FontSize="36" HorizontalAlignment="Center"/>
                            <TextBlock Text="Uleiuri &amp; Fluide" FontSize="16" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" Width="200" Height="120" CornerRadius="10" Margin="10" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="🌪️" FontSize="36" HorizontalAlignment="Center"/>
                            <TextBlock Text="Filtre Auto" FontSize="16" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" Width="200" Height="120" CornerRadius="10" Margin="10" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="🛑" FontSize="36" HorizontalAlignment="Center"/>
                            <TextBlock Text="Sistem Frânare" FontSize="16" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" Width="200" Height="120" CornerRadius="10" Margin="10" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="⚙️" FontSize="36" HorizontalAlignment="Center"/>
                            <TextBlock Text="Suspensie &amp; Direcție" FontSize="16" FontWeight="SemiBold" Foreground="#4A5568" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
