<Page x:Class="AutoPartsShop.AdminDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="760" d:DesignWidth="980"
      Title="AdminDashboardPage" Background="#F7FAFC" FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Dashboard Administrator" FontSize="28" FontWeight="Bold" Foreground="#1A202C"/>
                    <TextBlock Text="Rapoarte operationale, comenzi, facturi, plati si stocuri." FontSize="14" Foreground="#718096" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Genereaza raport" Click="BtnGenereazaRaport_Click" Style="{StaticResource ActionButton}" Background="#3182CE" Foreground="White"/>
                    <Button Content="Reincarca" Click="BtnReincarca_Click" Style="{StaticResource ActionButton}" Background="#2D3748" Foreground="White"/>
                </StackPanel>
            </Grid>

            <UniformGrid Columns="3" Margin="-8,0,-8,8">
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Utilizatori" Foreground="#718096"/>
                        <TextBlock x:Name="TxtTotalUtilizatori" Text="0" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Produse" Foreground="#718096"/>
                        <TextBlock x:Name="TxtTotalProduse" Text="0" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Stoc redus (max. 10 buc.)" Foreground="#718096"/>
                        <TextBlock x:Name="TxtStocRedus" Text="0" FontSize="26" FontWeight="Bold" Foreground="#C05621"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Comenzi" Foreground="#718096"/>
                        <TextBlock x:Name="TxtTotalComenzi" Text="0" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Facturi emise" Foreground="#718096"/>
                        <TextBlock x:Name="TxtFacturi" Text="0" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Venit incasat" Foreground="#718096"/>
                        <TextBlock x:Name="TxtVenit" Text="0.00 lei" FontSize="26" FontWeight="Bold" Foreground="#2F855A"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,8,8,8">
                    <StackPanel>
                        <TextBlock Text="Top produse vandute" FontSize="18" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,10"/>
                        <DataGrid x:Name="TopProduseGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="240" RowHeaderWidth="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Produs" Binding="{Binding Produs}" Width="2*"/>
                                <DataGridTextColumn Header="Cantitate" Binding="{Binding Cantitate}" Width="90"/>
                                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:0.00} lei}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,8,0,8">
                    <StackPanel>
                        <TextBlock Text="Ultimele comenzi" FontSize="18" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,10"/>
                        <DataGrid x:Name="ComenziRecenteGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="240" RowHeaderWidth="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data" Binding="{Binding DataComanda, StringFormat={}{0:dd.MM HH:mm}}" Width="100"/>
                                <DataGridTextColumn Header="Client" Binding="{Binding Client}" Width="*"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:0.00} lei}" Width="110"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource Card}" Margin="0,8,0,0">
                <StackPanel>
                    <TextBlock Text="Rapoarte generate" FontSize="18" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,10"/>
                    <DataGrid x:Name="RapoarteGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Height="190" RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Data" Binding="{Binding DataGenerare, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="145"/>
                            <DataGridTextColumn Header="Tip" Binding="{Binding TipRaport}" Width="160"/>
                            <DataGridTextColumn Header="Detalii" Binding="{Binding Detalii}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
