<Page x:Class="AutoPartsShop.PlatiFacturiPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="760" d:DesignWidth="980"
      Title="PlatiFacturiPage" Background="#F7FAFC" FontFamily="Segoe UI">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Plati si facturi" FontSize="26" FontWeight="Bold" Foreground="#1A202C"/>
                <TextBlock Text="Genereaza facturi si gestioneaza platile clientilor." FontSize="14" Foreground="#718096"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Reincarca" Click="BtnReincarca_Click" Style="{StaticResource ActionButton}" Background="#2D3748" Foreground="White" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12">
            <DataGrid x:Name="PlatiFacturiGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="PlatiFacturiGrid_SelectionChanged"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Factura" Binding="{Binding NumarFactura}" Width="130"/>
                    <DataGridTextColumn Header="Client" Binding="{Binding Client}" Width="170"/>
                    <DataGridTextColumn Header="Comanda" Binding="{Binding IDComanda}" Width="160"/>
                    <DataGridTextColumn Header="Data comanda" Binding="{Binding DataComanda, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="135"/>
                    <DataGridTextColumn Header="Total factura" Binding="{Binding TotalFactura, StringFormat={}{0:0.00} lei}" Width="110"/>
                    <DataGridTextColumn Header="Suma platita" Binding="{Binding SumaPlatita, StringFormat={}{0:0.00} lei}" Width="110"/>
                    <DataGridTextColumn Header="Metoda" Binding="{Binding Metoda}" Width="100"/>
                    <DataGridTextColumn Header="Status plata" Binding="{Binding StatusPlata}" Width="110"/>
                    <DataGridTextColumn Header="Status factura" Binding="{Binding StatusFactura}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,16,0,0">
            <StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="140"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Comanda pentru facturare" Foreground="#4A5568"/>
                        <ComboBox x:Name="CmbComenzi" Height="34" DisplayMemberPath="Afisare" SelectedValuePath="ID"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Suma" Foreground="#4A5568"/>
                        <TextBox x:Name="TxtSumaPlata" Height="34"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Metoda" Foreground="#4A5568"/>
                        <ComboBox x:Name="CmbMetoda" Height="34">
                            <ComboBoxItem Content="Card"/>
                            <ComboBoxItem Content="Cash"/>
                            <ComboBoxItem Content="Transfer"/>
                            <ComboBoxItem Content="Ramburs"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Status plata" Foreground="#4A5568"/>
                        <ComboBox x:Name="CmbStatusPlata" Height="34">
                            <ComboBoxItem Content="InAsteptare"/>
                            <ComboBoxItem Content="Platita"/>
                            <ComboBoxItem Content="Esuata"/>
                            <ComboBoxItem Content="Rambursata"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Genereaza factura" Click="BtnGenereazaFactura_Click" Style="{StaticResource ActionButton}" Background="#805AD5" Foreground="White"/>
                    <Button Content="Inregistreaza plata" Click="BtnInregistreazaPlata_Click" Style="{StaticResource ActionButton}" Background="#3182CE" Foreground="White"/>
                    <Button Content="Actualizeaza plata" Click="BtnActualizeazaPlata_Click" Style="{StaticResource ActionButton}" Background="#38A169" Foreground="White"/>
                    <Button Content="Sterge plata" Click="BtnStergePlata_Click" Style="{StaticResource ActionButton}" Background="#E53E3E" Foreground="White"/>
                    <Button Content="Anuleaza factura" Click="BtnAnuleazaFactura_Click" Style="{StaticResource ActionButton}" Background="#718096" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
